<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ci.mapper.ShoppingMapper">

    <resultMap id="ShoppingMap" type="com.ci.pojo.entity.Shopping">
        <id property="shoppingId" column="shopping_id"/>
        <result property="userId" column="user_id"/>
        <result property="orderId" column="order_id"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="receiverProvince" column="receiver_province"/>
        <result property="receiverCity" column="receiver_city"/>
        <result property="receiverDistrict" column="receiver_district"/>
        <result property="receiverAddress" column="receiver_address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <insert id="add">
        insert into goods (shopping_id, user_id,order_id,receiver_name,receiver_phone, receiver_province,receiver_city,receiver_district,receiver_address,create_time, update_time)
        values (#{shoppingId}, #{userId}, #{orderId}, #{receiverName}, #{receiverPhone}, #{receiverProvince}, #{receiverCity}, #{receiverDistrict}, #{receiverAddress}, #{createTime}, #{updateTime})
    </insert>

    <delete id="delete">
        delete from goods
        where good_id = #{goodId}
    </delete>

    <update id="updateGood">
        update goods
        <set>
            <if test="cateId != null">cate_id = #{cateId}</if>
            <if test="name != null">name = #{name}</if>
            <if test="subTitle != null">sub_title = #{subTitle}</if>
            <if test="mainImage != null">main_image = #{mainImage}</if>
            <if test="subImages != null">sub_images = #{subImages}</if>
            <if test="detail != null">detail = #{detail}</if>
            <if test="price != null">price = #{price}</if>
            <if test="stock != null">stock = #{stock}</if>
            <if test="status != null">status = #{status}</if>
            <if test="createTime != null">create_time = #{createTime}</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        where good_id = #{goodId}
    </update>

    <select id="getAll" resultMap="ShoppingMap">
        select *
        from goods
    </select>
    <select id="getByGoodId" resultMap="ShoppingMap">
        select *
        from goods
        where good_id = #{goodId}
    </select>

</mapper>